diff a/src/index.ts b/src/index.ts	(rejected hunks)
@@ -8 +8 @@ export enum SearchAlgorithmNodeBehavior {
-  break = 1,
+  break = 1
@@ -143 +143 @@ export class Graph<T> {
-    this._nodes.forEach((node) => {
+    this._nodes.forEach(node => {
@@ -149 +149 @@ export class Graph<T> {
-          incomingNeighbors: node.incomingNeighbors.slice(0),
+          incomingNeighbors: node.incomingNeighbors.slice(0)
@@ -161 +161 @@ export class Graph<T> {
-        const neighborNode = left.find((l) => l.id === id);
+        const neighborNode = left.find(l => l.id === id);
@@ -168 +168 @@ export class Graph<T> {
-          (n) => {
+          n => {
